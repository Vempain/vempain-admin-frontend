name: CI

permissions:
  contents: write
  packages: write

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  ci:
    uses: Vempain/vempain-workflows/.github/workflows/frontend-spa.yaml@main
    secrets: inherit
